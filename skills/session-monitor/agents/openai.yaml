interface:
  display_name: "Session Monitor"
  short_description: "Operate Knify's daemon that polls local Codex and Claude Code session files."
  default_prompt: "Use the session monitor daemon in apps/session-monitor to dry-run, watch, publish, or check local session review canvas state."
